---
import TaxonomyPage from '../../components/TaxonomyPage.vue';
import { siteConfig } from '../../data/site';
import BaseLayout from '../../layouts/BaseLayout.astro';
import { translate } from '../../lib/i18n';
import { loadTaxonomyTerms } from '../../lib/posts';

const terms = await loadTaxonomyTerms('category');
const metaParams = { count: terms.length };
const title = translate(siteConfig.i18n.defaultLocale, 'pages.categories.title', metaParams);
const description = translate(siteConfig.i18n.defaultLocale, 'pages.categories.description', metaParams);
---

<BaseLayout shellMode="blog" shellTitle={title} title={`${title} | ${siteConfig.title}`} description={description}>
    <TaxonomyPage
        client:idle
        variant="categories"
        titleKey="pages.categories.title"
        descriptionKey="pages.categories.description"
        metaParams={metaParams}
        terms={terms}
    />
</BaseLayout>
