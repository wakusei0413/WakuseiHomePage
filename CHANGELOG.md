# 更新日志

这里记录 Wakusei HomePage 每个版本真正影响使用体验的变化。

---

## [2.0.0] - 2026-09-20

# 主页已改，博客请阅。

Wakusei HomePage 2.0.0 正式发布。

老朋友们都知道，1.x 版本的它其实只是一张挂着头像、时钟、社交链接和底栏 Dock 的“单页名片”。每次切页面或换布局，还得盯着一个挡住全屏的 Loading 菊花图看上半天。

在 2.0.0 里，我们不仅把原本的主页从头到脚重新翻新了一遍，更是**从零搭建起了一整套现代、极速、真正适合沉浸阅读的静态博客系统**。

这是一次跨代的大升级。以下是对比 1.x 版本最核心的升级点与新特性：

---

### 🌟 核心升级与全新特性

#### 1. 从“单页名片”到“全功能博客系统”

1.x 里压根没有文章，也没有任何博客页面。2.0.0 彻底告别了纯展示页的局限，带来了完整的静态博客体验：

- **首页文章瀑布流与静态分页**：打开首页就能看到最新的文章卡片，阅读时长、发布日期、分类和标签一目了然，支持静态分页（`/page/2` 起）。
- **时间线归档（/archives）**：按年份倒序排列的时间线，记录所有的写作轨迹。
- **多维度分类检索**：支持分类（`/categories`）、标签（`/tags`）与专题话题（`/topics`），找特定方向的文章不再大海捞针。
- **RSS & Atom 全量分发**：支持标准 RSS 2.0（`/rss.xml`）与 Atom（`/atom.xml`），可以用任意喜欢的 RSS 阅读器订阅全站更新。
- **构建期图片自动瘦身**：所有文章封面和正文配图在打包时基于 Sharp 自动转码为高压缩比 WebP，长文加载再也不用心疼流量。

#### 2. 从零打造的长文阅读器

文章不仅要能看，还要读得舒服。我们在 2.0.0 里为文章页量身定做了一整套阅读辅助系统：

- **排版随心调节**：悬浮面板支持字号多档自由缩放（基准字号提升至 110%，告别眯眼看小字），版面宽度支持窄屏、标准、宽屏自由切换。
- **高对比度纯色目录（TOC）**：侧边悬浮目录背景采用高对比度纯色遮罩，彻底隔绝背景壁纸干扰；滚动正文时自动高亮当前章节，点击目录项平滑滚动直达。
- **一键沉浸阅读**：一键隐藏所有干扰元素，配合即时明暗主题切换，夜间读长文更护眼。
- **代码与链接一键复制**：代码块右上角自带复制按钮，带剪贴板兼容回退与即时反馈；文章内所有标题鼠标悬停即可复制直达锚点，分享具体章节更顺手。
- **画廊级图片灯箱**：正文插图点击直接弹窗放大，支持平滑缩放与细节查看。
- **顶部阅读进度条**：文章顶部常驻微进度条，看到哪里心里有数。
- **连读体验**：文章底部自带上一篇 / 下一篇直达导航，读完顺手翻下一篇。

#### 3. 扔掉全屏菊花 Loading，换成原生 App 般的无缝切页

1.x 每次进网站或者切布局都要看全屏 Loading 菊花图。2.0.0 直接把它扔进了垃圾桶：

- **首屏秒开**：彻底移除遮挡视线的全屏 Loading 遮罩，页面第一帧就呈现实质内容。
- **Astro 原生 View Transitions**：采用 Astro 7 的 `ClientRouter` 驱动全站跳转。精心调优了切页缓动曲线，点击文章、返回主页、浏览器前进后退时，页面像手机原生 App 一样丝滑滑入滑出，告别白屏和刺眼闪烁。
- **顶栏与壁纸常驻**：跨页面跳转时，顶栏与背景壁纸保持原位无缝衔接，没有重新加载的割裂感。

#### 4. 全局全键盘即时搜索

1.x 完全无法搜索内容。2.0.0 带来了一个随时可用的全局搜索系统：

- **毫秒级极速唤出**：支持全局快捷键唤出（或点击顶栏搜索按钮），无论是首页还是正在读文章，随叫随到。
- **正文级深搜**：同时检索文章标题、摘要和正文内容，中英文混搜、多关键词空格分隔搜索统统支持。
- **极客喜爱的全键盘盲操**：支持 `↑` / `↓` 切换结果、`Enter` 直接跳到文章、`Esc` 瞬间退出，焦点牢牢锁定在搜索框内，键盘党全程无需碰鼠标。
- **独立搜索页**：除了弹窗，还提供了全量的独立搜索页面（`/search`）。

#### 5. 壁纸会呼吸，页面有电影感

1.x 的壁纸功能相对单薄，手机端甚至为了防止卡顿直接跳过了壁纸加载。2.0.0 让壁纸真正有了质感：

- **Ken Burns 呼吸微动效**：基于 Web Animations API 打造，壁纸在后台缓慢而细腻地缩放呼吸。切换页面时自动平滑接管缩放相位，背景绝不会突然跳跃或重置。
- **智能色彩采样**：自动采样壁纸主色与对比度，毛玻璃面板与文字反色更加协调，保证在任何壁纸下文字都清晰可读。
- **智能竞态与优雅降级**：支持本地高清 WebP 壁纸与必应等外部 API 竞速加载；即便外部图床挂了，页面依然能在几毫秒内降级打开，绝不卡死。
- **防晕动症优化**：系统一旦开启“减少动态效果（prefers-reduced-motion）”，壁纸呼吸与过度动效立即收敛为静止，舒适不晕眩。

#### 6. 首页更丰富，也更有极客范儿

主页不再是一成不变的死板卡片，加入了更多实用与趣味细节：

- **GitHub 贡献热力图**：构建期自动抓取过去 53 周的 GitHub 提交记录并静态嵌入，无需配置个人 Token，在主页直接展示极客绿格子；接口离线时优雅降级。
- **流式跑马灯标语**：淘汰了 1.x 偶发抖动与卡顿的打字机动效，换成了平滑细腻的流式跑马灯，支持顺序轮播与随机切换。
- **三语国际化**：简体中文（`zh-CN`）、英语（`en`）、日语（`ja`）随时无刷新切换。

#### 7. 手机端体验翻新

针对 1.x 在移动端的各种小毛病进行了全面重构：

- **抽屉式导航**：移动端专属抽屉导航栏，单手滑动与点击更顺手。
- **防误触大触控区**：全站所有交互按钮与菜单项的触控热区均达到 44px 以上，严格符合 WCAG 无障碍规范，手指粗也不怕误点。
- **告别样式 BUG**：彻底修复了 1.x 移动端黑夜模式下透明头像变大白方块、分隔线粗细不一致、切换主题后图标不更新等顽疾。
- **折叠屏与平板适配**：在 899px ~ 901px 等临界断点下平滑自适应，没有恼人的横向溢出滚动条。

---

### ⚡ 给技术同好的底盘说明

如果你对底层技术感兴趣，这里是 2.0.0 的技术栈变动清单：

- **全面迁移到 Astro 7 + Vue 3 + Pinia**：
  - 彻底淘汰了 1.x 的 `Astro 5 + SolidJS 1.9`，交互组件全面重写为 Vue 3 `<script setup>` 组合式 API，配合 Pinia 做全局跨页状态管理。
  - **定制 Vue 运行时单例**：自研 `@astrojs/vue/client.js` 入口别名（`astro-vue-client.ts`），根治了多 island 水合时 Vue 实例与 Pinia 状态解绑的底层难题。
- **轻量化内联 SVG 图标系统**：
  - 移除了 1.x 动辄数百 KB 的 Font Awesome Webfont 字体包与 CSS，全站换成按需内联的 SVG 图标组件（`Icon.vue`），大幅降低首屏资源消耗。
- **Cloudflare 兼容防护**：
  - 构建产物自动注入 `data-cfasync="false"`，防止 Cloudflare 的 Rocket Loader 异步打乱脚本执行顺序而导致 Astro View Transitions 失效。
- **工程安全与测试门禁**：
  - 测试框架升级为 Vitest，包含 46 个测试文件、280 个测试用例，CI 覆盖依赖安全审计（`npm audit` 0 漏洞）与 177 个生成文件的静态发布产物检查。

---

### 🔄 从 1.x 升级配置注意事项

如果你只是阅读或部署默认站点，无需做任何修改；如果你维护了自己的 `src/data/customize.ts`，请留意以下变化：

1. **标语配置精简**：移除了旧版的 `slogans.typeSpeed` 与 `animation.cursorStyle`，现在使用 `slogans.mode`（`'sequence' | 'random'`）和 `slogans.pauseDuration` 控制跑马灯。
2. **顶栏导航项精简**：默认移除了无实际功能的“设置”占位链接，顶栏更加干净。
3. **调试日志**：生产环境默认将 `debug.consoleLog` 设为 `false`，保持控制台整洁。
4. **图标配置**：图标系统已内置常用 SVG，无需再引入外部字体文件。

---

### 📝 内容调整

- 撤下两篇历史草稿：`app-filing-and-internet-control` 与 `huaxue`。它们已设置为草稿状态，不会生成公开文章页面，也不会进入归档、分类、标签、搜索索引、RSS、Atom 或 Sitemap。

---

### 历史版本记录

- **2.0.0-alpha.4**：优化搜索键盘操作、触控热区和文字对比度；修复卡片悬停时文字发虚。
- **2.0.0-alpha.3**：加入首页跑马灯标语；优化壁纸和磨砂玻璃效果；增强安全响应头。
- **2.0.0-alpha.2**：完成博客内容管道、静态分页、文章归档和壁纸呼吸动效。
- **2.0.0-alpha.1**：完成从 SolidJS 到 Astro 7 + Vue 3 + Pinia 的核心架构重构。
