# 更新日志

这里记录 Wakusei HomePage 每个版本真正影响使用体验的变化。

---

## [2.0.0] - 2026-09-20

# 主页已改，博客请阅。

Wakusei HomePage 2.0.0 正式发布。

这次不是换个颜色、改几个按钮，而是把整个站点重新做了一遍：打开更快，文章更好读，搜索更顺手，手机上也更好用。你可以把它当成一个全新的版本来体验。

### 新增特性:

#### 1. 文章终于像一个真正的阅读器了

1.x 里，压根没有文章。2.0.0 里，我们文章不仅是“打开来看看”，更是把它做成了更适合长文阅读的样子：

- 可以调整字号、行高和文章宽度。
- 有目录和当前章节高亮，长文章不容易迷路。
- 有阅读进度条，看到哪里一目了然。
- 代码可以一键复制。
- 标题链接可以直接复制，分享文章里的某一节更方便。
- 图片可以点开看大图。
- 文章之间可以直接跳到上一篇和下一篇。
- 支持沉浸式阅读和明暗主题切换。

#### 2. 页面切换不再像“重新打开了一个网站”

从 1.x 到 2.0.0，站内导航换成了 Astro 原生的页面转场。点击文章、返回首页、前进后退时，页面状态衔接得更自然，不再频繁白屏，也不会每次都像第一次打开网站。

首页首屏也取消了会挡住内容的全屏 Loading，内容会更早出现。

#### 3. 搜索变成了真正能用的搜索

现在可以从顶部快速呼出搜索：

- 支持标题、摘要和正文搜索。
- 支持中文、英文和多个关键词一起搜。
- 可以用 `↑` / `↓` 选择结果，按 `Enter` 打开。
- 按 `Esc` 关闭，搜索框会自动管理焦点。
- 草稿文章不会混进搜索结果。

#### 4. 手机端重新整理过了

2.0.0 重新整理了移动端的侧边栏、按钮尺寸和触摸区域：

- 常用按钮更容易点中，触控区域达到 44px 以上。
- 侧边栏、搜索、主题和语言切换可以配合使用。
- 页面在手机、平板和桌面宽度之间切换时更稳定。
- 开启系统“减少动态效果”后，动画会自动收敛，不强行晃动。

#### 5. 壁纸不只是背景了

动态壁纸现在支持本地 WebP 壁纸和多个来源竞速加载。壁纸会有轻微的 Ken Burns 呼吸动效，切换页面时也会尽量保持连续，不再突然跳回原点。

如果外部壁纸服务不可用，页面仍然可以正常打开，不会因为一张背景图把整个首页卡住。

#### 6. 首页信息更多，但不会变成后台面板

首页新增和完善了：

- GitHub 过去 53 周的贡献热力图。
- 文章分页、归档、分类、标签和话题浏览。
- 更清楚的文章卡片信息和阅读时间。
- 中、英、日三种语言支持。
- 更统一的明暗主题体验。

### 从 1.x 到 2.0.0，底层也换了一遍

如果你只关心使用体验，可以跳过这一节。简单说，2.0.0 把旧版的基础设施换成了更容易维护、也更适合继续扩展的组合：

- 从旧的手写运行时迁移到 **Astro 7 + Vue 3 + Pinia + TypeScript**。
- 从 Svelte / Swup 迁移到 Vue Islands 和 Astro 原生页面转场。
- 博客内容改为 Markdown 驱动，文章、封面、搜索索引、RSS、Atom 和 Sitemap 都在构建时自动生成。
- 图片在构建时自动优化并生成 WebP，减少文章页的图片负担。
- 修复多个 Vue island 共存时可能出现的状态和水合问题。
- 针对 Cloudflare Rocket Loader 增加兼容处理，避免它打乱站点脚本执行顺序。
- CI 增加依赖安全检查和构建产物检查，不只看“能不能 build”，还会检查草稿有没有泄露到公开页面、Feed 和 Sitemap。

---
