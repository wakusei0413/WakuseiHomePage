# 博客文章页 UI/UX 与 Markdown 阅读体验审查报告

> 审查范围：文章阅读体验与 Markdown 渲染 (`/posts/[...slug]` 及其相关样式、交互)
> 审查日期：2026-06-21
> 审查方式：代码走查 + 本地 Dev Server 实机测试（桌面 1036px / 移动 375px）

---

## 执行摘要（先看这里）

当前文章页已经具备了一套完整的 Markdown 渲染骨架：标题、代码块、表格、引用、列表、任务列表、图片、分隔线等基础元素都有样式。但整体**可读性、信息层级、移动端适配、交互反馈**上仍存在不少会让读者“看着累、用着卡”的问题。

我将问题按严重程度分为三级：

- **🔴 P0（严重）**：会直接影响阅读、理解或操作，建议优先修改。
- **🟠 P1（中等）**：会让体验打折扣，但还能凑合用，建议第二阶段处理。
- **🟢 P2（建议）**：属于锦上添花或长期优化，可根据精力排期。

---

## 目录

1. [全局与页面结构问题](#1-全局与页面结构问题)
2. [标题层级与信息架构问题](#2-标题层级与信息架构问题)
3. [正文排版与可读性问题](#3-正文排版与可读性问题)
4. [代码块体验问题](#4-代码块体验问题)
5. [表格体验问题](#5-表格体验问题)
6. [列表与任务列表问题](#6-列表与任务列表问题)
7. [链接、引用与辅助元素问题](#7-链接引用与辅助元素问题)
8. [移动端特有问题](#8-移动端特有问题)
9. [暗色模式问题](#9-暗色模式问题)
10. [无障碍与交互问题](#10-无障碍与交互问题)
11. [修改优先级总表](#11-修改优先级总表)

---

## 1. 全局与页面结构问题

### 1.1 页面 H1 标题出现在 Hero 区，而非文章正文区

- **优先级**：🔴 P0
- **问题描述**：文章标题被渲染在左侧 Hero 面板（`.left-panel .name`）里，而不是 `.post-container` 内部。正文区 `<article class="post-container">` 的第一层几乎没有“这是哪篇文章”的清晰标识。
- **为什么难受**：
  - 进入文章页时，用户的视线焦点本应是标题，但标题被挤到左侧小面板，视觉权重低。
  - 搜索引擎和屏幕阅读器的文档大纲没问题，但对普通读者来说，正文入口感弱。
- **具体示例**：打开 `/posts/huaxue`，左侧小面板显示“有机化学·官能团与反应类型”，右侧大片区域却直接从 `## 上海高考·有机推断大题` 开始。
- **修改建议**：
  - 在 `.post-container` 内部的 `<header class="post-header">` 里把 `frontmatter.title` 以 `<h1 class="post-title">` 形式重新渲染。
  - Hero 区可以保留作者名/站点名，不必重复文章标题。
  - 参考结构：
    ```html
    <article class="post-container">
      <header class="post-header">
        <h1 class="post-title">{frontmatter.title}</h1>
        <div class="post-meta">
          {frontmatter.tags && <span class="tags">{...}</span>}
          {frontmatter.date && <time datetime="...">{...}</time>}
        </div>
      </header>
      ...
    </article>
    ```

### 1.2 缺少发布日期与更新时间

- **优先级**：🔴 P0
- **问题描述**：文章 `frontmatter` 没有显示日期，也缺少相关字段（目前只有 `tags`）。对博客读者来说，时间是非常重要的信任信号。
- **为什么难受**：
  - 技术类文章没有日期，读者无法判断内容是否过时。
  - 教学类文章（如化学教程）如果有“最后更新”会更有说服力。
- **具体示例**：`hello-world/index.md` 和 `huaxue/index.md` 都没有 `pubDate` 或 `date`，列表页和文章页都不显示时间。
- **修改建议**：
  - 在 Markdown frontmatter 中约定 `pubDate` 和 `updatedDate` 字段。
  - 在文章头部增加 `<time>` 标签：
    ```html
    <time class="post-date" datetime="2026-06-21">2026年6月21日</time>
    ```
  - 格式建议：`2026年6月21日 · 约 15 分钟读完`

### 1.3 左侧 Hero 面板占据大量固定宽度，正文栏被压缩

- **优先级**：🟠 P1
- **问题描述**：桌面端 `.left-panel` 宽度 500px（断点 1200px 以下也占 35%）。在 1280px 以下笔记本屏幕上，正文可用宽度会被明显挤压。
- **为什么难受**：
  - 写文章是给读者读的，正文才是主角，侧边栏不应喧宾夺主。
  - 当前桌面实测：视口 1036px，正文区可用约 672px，而标题区 logo+名字占掉 362px。
- **具体示例**：在 13 寸笔记本半屏（约 1024px）下阅读，每行字数明显偏少。
- **修改建议**：
  - 将文章页的 `.left-panel` 在桌面端也改为顶部紧凑栏（与首页可保持不同布局），或至少缩窄至 280px 并支持收起。
  - 文章页正文最大宽度建议从 720px 提升到 `min(760px, 100% - 48px)`。

### 1.4 页面加载动画遮挡首屏，且无法跳过

- **优先级**：🟠 P1
- **问题描述**：首次进入或刷新时，全屏 loading overlay 会显示进度条和文字，必须等 `homepageReady` / `shellReady` 或 10 秒超时。
- **为什么难受**：
  - 文章页不是首页，不需要等壁纸加载完才能看文字。
  - 实测在 dev 模式下，有时 overlay 会短暂闪现，打断阅读流。
- **具体示例**：直接访问 `/posts/hello-world` 时，`globalLoadingOverlay` 仍会参与。
- **修改建议**：
  - 文章页（`shellMode !== 'home'`）应跳过全屏 entry loader，或立即隐藏 overlay。
  - 代码位置：`BaseLayout.astro` 内联脚本 `isIncomingHomePage` 判断。

### 1.5 文章封面图缺少合理的尺寸控制

- **优先级**：🔴 P0
- **问题描述**：当前 `.post-hero-image` 使用 `width: 100%; height: auto;`，会按原图比例展示。如果作者上传了一张正方形 logo（如 `hello-world` 的 `/res/img/logo.png`），封面会变成巨大方块。
- **为什么难受**：
  - 阅读体验瞬间崩坏：一张 672×672 的 logo 会把首屏塞满，正文被推到很下方。
  - 封面图应当是“横幅”语义，而不是原图直接放大。
- **具体示例**：`/posts/hello-world` 的封面 logo 在桌面端显示为 672×672 的大方块，远超“文章配图”的预期。
- **修改建议**：
  - 给 `.post-hero-image` 增加固定比例容器：
    ```css
    .post-hero-image-wrapper {
      width: 100%;
      aspect-ratio: 16 / 9;
      overflow: hidden;
      border-radius: 6px;
    }
    .post-hero-image-wrapper img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
    ```
  - 对没有合适封面的小图/ logo 文章，建议在 frontmatter 增加 `showCover: false` 或自动判断图片比例。

---

## 2. 标题层级与信息架构问题

### 2.1 文章内的 H1 与页面 H1 重复，造成视觉和语义混乱

- **优先级**：🔴 P0
- **问题描述**：`huaxue/index.md` 正文里用了 `# 站 0：建立框架` 这样的 H1。而整篇文章的页面 H1 已经是 frontmatter 标题。
- **为什么难受**：
  - Markdown 正文中，H1 应当 reserved for 文章标题。读者突然看到另一个 H1 会 confused：这篇文章到底有几个主标题？
  - 对目录/大纲生成也不友好，H1 会被当成顶级节点。
- **具体示例**：`huaxue` 文章中 `# 站 0：建立框架`、`# 站 1：碳碳双键 C=C` 等多次出现 H1。
- **修改建议**：
  - 内容规范：正文最高只到 `##`，把现有的 `# 站 X` 改为 `## 站 X`，`## 入场测` 改为 `### 入场测`，依此类推降级一级。
  - 可在构建时增加 lint 检查：Markdown 正文中不允许出现 H1。

### 2.2 标题前导装饰色块风格不统一

- **优先级**：🟢 P2
- **问题描述**：
  - `h1` 左侧是蓝色粗竖线（`border-left: 4px solid var(--accent-blue)`）。
  - `h3` 左侧是黄色粗竖线。
  - `h2` 却是上边框分隔线，风格与前两者不同。
- **为什么难受**：同一篇文章里出现三种标题装饰语言，读者需要额外认知成本。
- **具体示例**：`h2` 的顶部横线看起来像“分隔线”，而非“标题”。
- **修改建议**：
  - 统一用一种视觉语言：左侧色块。
  - `h1`：蓝色 4px；`h2`：蓝色 3px；`h3`：黄色 3px；`h4` 及以下使用纯文字层级。
  - 去掉 `h2` 的 `border-top`，改为 `border-left`，视觉更连贯。

### 2.3 标题字号递进过大

- **优先级**：🟠 P1
- **问题描述**：当前文章内标题字号：h1 `38.4px`（clamped）、h2 `24px`、h3 `19.2px`。h1→h2 落差接近 1.6 倍，h2→h3 落差仅 1.25 倍，层级节奏不均匀。
- **为什么难受**：h1 过于巨大，h2 又略显小，导致中级章节不够突出。
- **具体示例**：`huaxue` 的 `# 站 0` 比 `## 核心内容` 视觉上大太多。
- **修改建议**：
  - 推荐比例：h1 `2rem`（32px）、h2 `1.6rem`（约 26px）、h3 `1.3rem`（约 21px）、h4 `1.15rem`。
  - 或统一使用 `1.4 / 1.25 / 1.15` 的递进比例。

### 2.4 锚点链接（#）只在大标题出现，且默认隐藏

- **优先级**：🟢 P2
- **问题描述**：锚点链接只跟随标题，鼠标 hover 才出现。
- **为什么难受**：移动端没有 hover，无法复制/跳转锚点。
- **修改建议**：
  - 锚点始终显示，但降低对比度（如 `opacity: 0.3`）。
  - 或至少让 `h2`、`h3` 标题可点击跳转。

---

## 3. 正文排版与可读性问题

### 3.1 行高略显紧凑，长文易产生压迫感

- **优先级**：🟠 P1
- **问题描述**：`.post-body` 行高 `1.75`（17px 字号下约 29.75px），对中文来说刚好及格，但对长段教程略显局促。
- **为什么难受**：`huaxue` 文章有大量知识点、表格、代码，段落与段落之间区分度不够，读起来像“堆在一起”。
- **具体示例**：连续几段解释文字后，读者容易迷失位置。
- **修改建议**：
  - 中文长文建议行高 `1.8` 至 `1.9`。
  - 段落间距当前 `margin-bottom: var(--space-md)`（24px），对中文可增加至 `1.8rem`（约 29px）或配合 `text-indent` / 段落首字下沉等节奏设计。

### 3.2 正文颜色与背景对比度足够，但暖色背景长时间阅读偏累

- **优先级**：🟢 P2
- **问题描述**：`--bg: #fffef7`（暖白），`--fg: #0a0a0a`。对比度优秀，但暖色调长时间看纯文本容易眼疲劳。
- **为什么难受**：尤其是代码块、表格也带有边框/阴影时，暖底会让整体显得“脏”。
- **修改建议**：
  - 文章页可单独使用更冷的背景（如 `#ffffff` 或 `#f9fafb`），与站点首页的个性背景区分开。
  - 或在暗色模式下默认启用（深色背景对阅读更友好）。

### 3.3 段落 margin-bottom 为 0，排版节奏依赖 line-height

- **优先级**：🟠 P1
- **问题描述**：`.post-body p { margin: 0 0 var(--space-md); }`，看起来没问题，但检查 computed style 时发现 `margin-bottom` 实际为 `0px`。
- **为什么难受**：说明 `.post-body p` 的这条规则可能被其他样式覆盖或单位解析异常，导致段落之间没有额外间距，全靠行高撑开，读起来拥挤。
- **具体示例**：实机测量 `window.getComputedStyle(document.querySelector('.post-body p')).marginBottom === '0px'`。
- **修改建议**：
  - 检查 `article.css` 中段落 margin 的覆盖来源，确保段落间距真实生效。
  - 推荐值：`.post-body p { margin-bottom: 1.25em; }`。

### 3.4 字体在文章页仍然是 UI 风格无衬线

- **优先级**：🟠 P1
- **问题描述**：`.post-body` 使用的是 `var(--font-ui)`，即 Inter / Noto Sans SC 无衬线字体。标题才用衬线字体。
- **为什么难受**：
  - 无衬线正文适合短资讯，但长文阅读（尤其中文）衬线字体更省力。
  - 项目里已经定义了 `--font-serif: 'Times New Roman', 'Noto Serif SC', 'Songti SC', 'SimSun', serif`，但没有在正文使用。
- **具体示例**：整篇文章都是无衬线，像在看 UI 说明文档，而不是博客。
- **修改建议**：
  - 将 `.post-body` 的 `font-family` 改为 `var(--font-serif)` 或 `var(--font-serif)` 与 `var(--font-ui)` 组合：
    ```css
    .post-body {
      font-family: var(--font-serif);
    }
    .post-body code,
    .post-body pre,
    .post-body table,
    .post-body kbd {
      font-family: var(--font-ui);
    }
    ```

---

## 4. 代码块体验问题

### 4.1 代码块字号偏小，与正文脱节

- **优先级**：🔴 P0
- **问题描述**：`pre` 字号 `14px`，`code` 字号 `12.6px`，而正文 `17px`。比例约为 0.74，偏低。
- **为什么难受**：
  - 读者需要凑近看代码。
  - 对含中文注释的代码，小字号+等宽字体会更难辨认。
- **具体示例**：`huaxue` 中大量 ASCII 结构图（如乙烯结构）用 12.6px 显示，化学符号糊成一团。
- **修改建议**：
  - 代码块字号建议至少 `0.9em`（约 15.3px）或直接 `15px`。
  - `pre code` 不要比 `pre` 更小；统一 `font-size: 0.9375rem`。

### 4.2 代码块与正文之间缺少“场景感”区分

- **优先级**：🟠 P1
- **问题描述**：代码块使用白色背景 + 细边框，对亮色主题来说太平淡，不容易一眼定位。
- **为什么难受**：文章中代码块很密集时，读者难以快速判断“这是代码还是正文”。
- **具体示例**：`huaxue` 里化学结构图和文字混排时，结构图看起来像是普通加粗文本。
- **修改建议**：
  - 给代码块增加左侧强调线（类似引用块），例如 `border-left: 3px solid var(--accent-blue)`。
  - 或给代码块背景使用 subtle tint：`background: color-mix(in srgb, var(--accent-blue) 5%, #ffffff)`。

### 4.3 代码块横向滚动条被全局隐藏

- **优先级**：🔴 P0
- **问题描述**：`base.css` 中 `*::-webkit-scrollbar { display: none; }` 和 `scrollbar-width: none` 是全局的，导致 `pre` 即使设置了 `overflow-x: auto`，用户也看不到滚动条。
- **为什么难受**：
  - 读者不知道代码块可以横向滚动，长代码被截断也毫无提示。
  - 触屏设备更难发现可滚动区域。
- **具体示例**：`huaxue` 中部分化学方程式较长，若超出容器则会被截断。
- **修改建议**：
  - 仅对 body/html 隐藏滚动条，不要对 `pre`、`.post-body` 隐藏。
  - 为代码块恢复滚动条样式：
    ```css
    .post-body pre::-webkit-scrollbar {
      display: block;
      height: 6px;
    }
    .post-body pre::-webkit-scrollbar-thumb {
      background: var(--muted);
      border-radius: 3px;
    }
    ```

### 4.4 复制按钮在移动端始终显示，遮挡代码

- **优先级**：🟠 P1
- **问题描述**：`@media (hover: none)` 下 `.code-copy { opacity: 1; }`，移动端按钮常驻右上角。
- **为什么难受**：
  - 小屏幕代码块本身就很窄，按钮常驻会挡住约 48×24 px 的代码区域。
  - 对纯展示型代码（如化学结构图），复制按钮意义不大。
- **具体示例**：`huaxue` 移动端中，化学结构图的复制按钮盖住了右上角字符。
- **修改建议**：
  - 移动端将复制按钮移到代码块外部底部，或默认半透明，点击代码块区域再出现。
  - 或改为只在有 `language-*` 类时才显示复制按钮（Shiki 会给代码块加 `language-*`）。

### 4.5 代码块缺少语言标签

- **优先级**：🟢 P2
- **问题描述**：代码块上方没有显示“是什么语言”，也缺少文件名提示。
- **为什么难受**：读者无法快速判断代码类型，对多语言教程尤其不便。
- **修改建议**：
  - 读取 Shiki 生成的 `language-*` class，渲染一个小标签在左上角：
    ```css
    .post-body .code-block::before {
      content: attr(data-language);
      ...
    }
    ```

---

## 5. 表格体验问题

### 5.1 表格列宽由内容自动撑开，中文对齐不稳定

- **优先级**：🟠 P1
- **问题描述**：表格 `width: 100%`，单元格靠内容撑开。当某一列内容长短差异大时，会出现大面积空白或挤压。
- **为什么难受**：`huaxue` 中“一句话定义”列文字很长，“能/否”列只有 emoji，导致后者被挤得很窄。
- **具体示例**：`五种反应类型 · 一张卡片` 表格中，“一句话定义”列占满后，“标志”列只剩一点点空间。
- **修改建议**：
  - 对常见表格增加 `table-layout: fixed`，并通过 `:nth-child` 设置列宽比例。
  - 或增加 `min-width` 给单元格，保证短列不被压扁。

### 5.2 表格缺少斑马纹或悬停高亮

- **优先级**：🟢 P2
- **问题描述**：表格只有底部细线分隔，行与行之间没有背景区分。
- **为什么难受**：宽表格跨行阅读时，眼睛容易串行。
- **修改建议**：
  - 增加斑马纹：
    ```css
    .post-body tbody tr:nth-child(even) {
      background: color-mix(in srgb, var(--fg) 3%, transparent);
    }
    ```
  - 行悬停时轻微高亮。

### 5.3 表格缺少 caption 语义

- **优先级**：🟢 P2
- **问题描述**：文章用 `### 六个官能团 · 一张卡片` 这种 H3 作为表格标题，而不是 `<caption>`。
- **为什么难受**：打印或 RSS 导出时，表格标题与表格分离。
- **修改建议**：
  - 在 Markdown 中支持表格 caption 插件，或在构建时把表格前紧邻的 H3/H4 提取为 caption。

---

## 6. 列表与任务列表问题

### 6.1 任务列表（checkbox）不可交互但看起来像可点击

- **优先级**：🔴 P0
- **问题描述**：`.task-list-item input[type='checkbox']` 设置了 `cursor: default`、`appearance: none`，但保留了 checkbox 视觉。
- **为什么难受**：
  - 用户会下意识去点击，却发现点不动，产生挫败感。
  - 无法交互的任务列表，不如用纯视觉符号（✓/○）表达进度。
- **具体示例**：`huaxue` 每个“站 X 检查点”都有一排 checkbox，手机上读者会去点。
- **修改建议**：
  - 方案 A：让 checkbox 真正可交互（用 JS 把状态存到 `localStorage`，刷新后保持）。
  - 方案 B：如果不需要交互，改为静态图标（`.task-done` / `.task-todo`），明确表达“这是清单，不是表单”。
  - 推荐方案 B，对教学文章更稳妥。

### 6.2 列表项间距与行高不一致

- **优先级**：🟢 P2
- **问题描述**：`.post-body li` 行高 `1.7`（约 28.9px），但 `margin-bottom: var(--space-xs)`（8px）。列表内文字与正文行高略有差异。
- **为什么难受**：列表项看起来像“悬浮”在段落之间，节奏不统一。
- **修改建议**：
  - 列表项行高与正文保持一致 `1.75`。
  - 列表项间距改为 `0.5em`，视觉上更紧凑。

---

## 7. 链接、引用与辅助元素问题

### 7.1 链接 underline 与 hover 背景叠加，略显花哨

- **优先级**：🟢 P2
- **问题描述**：`.post-body a` 有下划线 + hover 蓝色背景。
- **为什么难受**：下划线本身已经够清晰，hover 再加背景会让链接区域像“按钮”，分散注意力。
- **具体示例**：长段文字中多个链接时，hover 一片蓝。
- **修改建议**：
  - 简化 hover 效果：只加深下划线颜色，或把 `text-decoration-thickness` 从 1.5px 调到 2px。
  - 去掉 hover 背景。

### 7.2 引用块（blockquote）与正文区分度不足

- **优先级**：🟠 P1
- **问题描述**：blockquote 左侧蓝色竖线 + 淡蓝背景，但内文字号、行高与正文完全一致。
- **为什么难受**：重要提示/总结被淹没在正文里，读者难以快速识别“这是作者特别强调的内容”。
- **具体示例**：`huaxue` 中的“⚡ 先告诉你一件事”和“认知锚点”如果更突出，会大幅提升复习效率。
- **修改建议**：
  - 引用块文字稍微缩小/放大一点点（如 `font-size: 1.05rem`），或左侧加粗竖线。
  - 对不同类型引用增加语义类名：
    - `> ⚡ ...` → `.callout-tip`
    - `> 🚨 ...` → `.callout-warning`
    - `> 🧠 ...` → `.callout-note`
  - 用背景色区分：tip 黄色、warning 红色、note 蓝色。

### 7.3 分隔线（`<hr>`）是圆点，像加载失败

- **优先级**：🟢 P2
- **问题描述**：`.post-body hr` 被设计成居中的几个小圆点。
- **为什么难受**：对部分读者来说，圆点分隔线看起来像列表点或排版错误，不明确表达“章节结束”。
- **具体示例**：`huaxue` 中每个“站”之间用 `<hr>` 分隔，圆点反复出现。
- **修改建议**：
  - 改为更常见的细横线，或保留圆点但增加间距/说明。
  - 示例：
    ```css
    .post-body hr {
      border: none;
      height: 1px;
      background: var(--panel-border);
      margin: 2.5rem 0;
    }
    ```

---

## 8. 移动端特有问题

### 8.1 移动端正文宽度被 padding 吃掉，阅读区域太窄

- **优先级**：🔴 P0
- **问题描述**：移动端 `.post-container { padding: 0 24px; }`，在 375px 视口下正文可用宽度仅约 295px。
- **为什么难受**：
  - 每行只有 15-18 个汉字，远低于舒适的 25-35 字/行。
  - 表格、代码块在这个宽度下几乎全部需要横向滚动。
- **具体示例**：iPhone SE 尺寸下打开 `/posts/huaxue`，正文区很窄，眼睛需要频繁换行。
- **修改建议**：
  - 移动端 padding 缩小到 `16px` 或 `12px`。
  - 正文最大宽度在移动端改为 100%，不要额外限制。
  - 代码块、表格可做成全宽出血（`margin-left: -16px; margin-right: -16px; width: calc(100% + 32px)`）。

### 8.2 表格在移动端几乎不可读

- **优先级**：🔴 P0
- **问题描述**：表格 `font-size: 0.9375rem`（15px），在 295px 宽度下多列表格内容严重挤压。
- **为什么难受**：`huaxue` 的表格本身列数多（3-4 列），移动端需要横向滚动，但滚动条被隐藏。
- **具体示例**：“五种反应类型”表格在 iPhone 模拟器下，部分单元格只剩几个字，行高也被压扁。
- **修改建议**：
  - 对 3 列以上表格，移动端改为卡片式布局（每行一张卡片，列标签以 `<label>` 形式显示）。
  - 或至少允许表格横向滚动并显示滚动提示。

### 8.3 代码块在移动端字号太小且需要横向滚动

- **优先级**：🔴 P0
- **问题描述**：移动端代码块 `pre` 宽度约 295px，字号 14px，化学结构图字符多，难以完整显示。
- **为什么难受**：化学式 `CH₃CH₂—OH + H—Br → CH₃CH₂—Br + H₂O` 在窄屏下会折行或截断。
- **修改示例**：`/posts/huaxue` 中多处反应方程式在移动端阅读困难。
- **修改建议**：
  - 移动端代码块字号降至 `13px` 仍可接受，但行高需保持 `1.7`。
  - 代码块做成全宽出血，并允许横向滚动。
  - 对化学结构图这类 ASCII art，考虑使用 SVG/MathJax 替代纯文本。

### 8.4 顶部 TopBar 在移动端占据固定高度，但内容区没有预留空间

- **优先级**：🟠 P1
- **问题描述**：移动端 `.top-bar { position: fixed; height: 72px; }`，而 `#pageScroller` / `.page-transition-surface` 没有 `padding-top` 补偿。
- **为什么难受**：文章顶部内容会被 TopBar 遮挡约 72px，H1 标题也部分被盖住。
- **具体示例**：移动端打开文章，顶部的“有机化学·官能团与反应类型”标题会被导航栏遮住一部分。
- **修改建议**：
  - 移动端 `.page-transition-surface { padding-top: 72px; }`。
  - 或在 `.top-bar` 使用 `position: sticky` 而非 `fixed`。

### 8.5 移动端汉堡菜单侧栏缺少“返回文章”相关入口

- **优先级**：🟢 P2
- **问题描述**：移动端侧栏只有站点 Dock 项目（主页、文章、主题、语言等），没有文章内快速操作。
- **为什么难受**：长文章阅读时，用户想快速返回博客列表或回到顶部，需要滚动到文章底部找 `.post-footer` 的链接。
- **修改建议**：
  - 在文章页增加浮动的“返回顶部 / 返回列表”按钮（FAB）。
  - 或在 TopBar 右侧的移动端菜单中增加当前上下文操作。

---

## 9. 暗色模式问题

### 9.1 代码块暗色背景覆盖可能不完整

- **优先级**：🟠 P1
- **问题描述**：`.post-body pre` 在亮色模式下背景是 Shiki 内联样式 `#ffffff`；暗色模式下通过 `[data-theme='dark'] .post-body .astro-code { background-color: var(--shiki-dark-bg) !important; }` 覆盖。
- **为什么难受**：如果 Shiki 主题配置变化，或某个 token 没有 `--shiki-dark-bg`，代码块会出现“白底黑字”的刺眼组合。
- **具体示例**：实测切到暗色后，代码块背景变为深色，但 pre 边框仍使用 `--panel-border`（暗色下 `rgba(255,255,255,0.12)`），对比度偏低。
- **修改建议**：
  - 暗色模式下给 pre 增加更明显的外边框或 glow。
  - 统一在 `.post-body pre` 上设置 `background: var(--code-bg)`，让亮/暗色分别走 token。

### 9.2 引用块在暗色模式下背景过浅

- **优先级**：🟢 P2
- **问题描述**：引用块背景使用 `color-mix(in srgb, var(--accent-blue) 6%, transparent)`，暗色下几乎不可见。
- **为什么难受**：暗色主题中，高亮提示区域需要比正文背景稍亮或带边框才能被注意到。
- **修改建议**：
  - 暗色下引用块背景改为 `rgba(255,255,255,0.05)` 并加左侧色线。

---

## 10. 无障碍与交互问题

### 10.1 复制按钮缺少键盘聚焦样式

- **优先级**：🟠 P1
- **问题描述**：`.code-copy` 悬停时显示，但没有 `:focus-visible` 样式。
- **为什么难受**：键盘用户无法知道当前聚焦在哪个复制按钮上。
- **修改建议**：
  - 增加：
    ```css
    .post-body .code-copy:focus-visible {
      opacity: 1;
      outline: 2px solid var(--accent-blue);
      outline-offset: 2px;
    }
    ```

### 10.2 图片缺少 caption / figcaption

- **优先级**：🟢 P2
- **问题描述**：Markdown 图片只渲染为 `<img>`，没有 `<figure>` 和 `<figcaption>`。
- **为什么难受**：教程类图片（化学结构图、流程图）缺少说明文字，读者不知道图在表达什么。
- **修改建议**：
  - 支持 Markdown 图片 title 作为 caption：
    ```markdown
    ![结构图](c2h4.svg "图 1：乙烯的平面结构")
    ```
  - 渲染为：
    ```html
    <figure>
      <img src="..." alt="结构图" />
      <figcaption>图 1：乙烯的平面结构</figcaption>
    </figure>
    ```

### 10.3 文章页 Footer 与站点 Footer 视觉风格割裂

- **优先级**：🟢 P2
- **问题描述**：`.post-footer` 是站点内的返回链接，`.site-footer` 是全站深色页脚。两者紧挨着，但风格完全不同。
- **为什么难受**：读者滑到文章末尾，会突然进入一块深色大区域，心理上有“跳出感”。
- **修改建议**：
  - 让 `.site-footer` 在文章页使用与正文一致的背景色，或把 `.post-footer` 做得更像“章节结束页”的轻量导航。
  - 或者在文章页去掉深色大 footer，仅保留紧凑的版权栏。

---

## 11. 修改优先级总表

| 优先级 | 问题 | 影响 | 建议改动文件 |
| --- | --- | --- | --- |
| 🔴 P0 | 文章标题未在正文区 H1 显示 | 信息入口弱 | `[...slug].astro`, `article.css` |
| 🔴 P0 | 缺少发布/更新时间 | 信任感缺失 | `types/site.ts`, `customize.ts`, `[...slug].astro` |
| 🔴 P0 | 封面图无尺寸控制，正方形图会撑爆首屏 | 视觉灾难 | `article.css`, `[...slug].astro` |
| 🔴 P0 | 正文 H1 滥用（内容里出现多个 H1） | 层级混乱 | 内容规范 + lint |
| 🔴 P0 | 全局隐藏滚动条导致代码块/表格可滚动无提示 | 内容被截断 | `base.css`, `article.css` |
| 🔴 P0 | 任务列表 checkbox 不可交互但像可点击 | 挫败感 | `article.css` + JS |
| 🔴 P0 | 移动端正文区过窄（295px） | 阅读极累 | `article.css`, `responsive.css` |
| 🔴 P0 | 移动端表格可读性差 | 核心内容看不清 | `article.css`, `responsive.css` |
| 🔴 P0 | 移动端代码块过小/需要横向滚动 | 公式结构图看不清 | `article.css`, `responsive.css` |
| 🟠 P1 | 左侧 Hero 面板压缩正文宽度 | 桌面阅读局促 | `BaseLayout.astro`, `layout.css` |
| 🟠 P1 | 文章页 loading overlay 没必要 | 进入被打断 | `BaseLayout.astro` |
| 🟠 P1 | 标题层级字号递进不均 | 章节不突出 | `article.css` |
| 🟠 P1 | 行高紧凑、段落 margin 疑似失效 | 长文压迫感 | `article.css` |
| 🟠 P1 | 正文使用无衬线字体 | 长文阅读累 | `article.css` |
| 🟠 P1 | 代码块字号偏小 | 需要凑近看 | `article.css` |
| 🟠 P1 | 代码块与正文视觉区分弱 | 定位困难 | `article.css` |
| 🟠 P1 | 复制按钮在移动端遮挡代码 | 小屏内容被挡 | `article.css`, `copy-code.ts` |
| 🟠 P1 | 表格列宽不稳 | 中文对齐差 | `article.css` |
| 🟠 P1 | 引用块提示不够突出 | 重点被淹没 | `article.css` + remark 插件 |
| 🟠 P1 | 移动端 TopBar 遮挡顶部内容 | H1 被盖住 | `responsive.css` |
| 🟠 P1 | 复制按钮无键盘聚焦 | 无障碍缺失 | `article.css` |
| 🟠 P1 | 暗色代码块背景覆盖风险 | 刺眼组合 | `article.css` |
| 🟢 P2 | 标题装饰风格不统一 | 视觉语言杂 | `article.css` |
| 🟢 P2 | 锚点链接默认隐藏 | 移动端难用 | `article.css` |
| 🟢 P2 | 代码块缺少语言/文件名标签 | 信息缺失 | `copy-code.ts`, `article.css` |
| 🟢 P2 | 表格缺少斑马纹/悬停高亮 | 易串行 | `article.css` |
| 🟢 P2 | 表格缺少 caption | 语义弱 | remark 插件 |
| 🟢 P2 | 列表项行高与正文不一致 | 节奏乱 | `article.css` |
| 🟢 P2 | 链接 hover 背景花哨 | 分散注意力 | `article.css` |
| 🟢 P2 | `<hr>` 圆点分隔线表意不明 | 像排版错误 | `article.css` |
| 🟢 P2 | 暖色背景长时间阅读累 | 眼疲劳 | `base.css` / 文章页单独背景 |
| 🟢 P2 | 移动端缺少返回顶部/列表快捷按钮 | 操作不便 | 新增 FAB 组件 |
| 🟢 P2 | 暗色引用块背景过浅 | 提示不明显 | `article.css` |
| 🟢 P2 | 图片缺少 caption | 图示不明 | remark 插件 |
| 🟢 P2 | 文章页 Footer 与站点 Footer 风格割裂 | 跳出感 | `footer.css`, `article.css` |

---

## 结语

作为一个刚起步的博客，最重要的是让读者“愿意读下去”。当前文章页的最大风险不是缺动画或缺特效，而是**正文区的信息层级、可读性、移动端适配**还不够扎实。

建议分两轮修改：

1. **第一轮（本周）**：处理所有 P0 问题，尤其是标题位置、时间、封面图尺寸、H1 降级、滚动条、任务列表、移动端宽度。
2. **第二轮（下周）**：处理 P1 和 P2，优化字体、字号、引用块、表格、暗色模式、图片 caption 等细节。

如果你愿意，我可以继续帮你：

- 把这份报告中的高优先级问题直接改写成代码并提交；
- 或者先为某一条（如“文章页标题重构”）做一个具体的设计方案，等你确认后再动手。
